import 'package:flutter/material.dart';

class StudentProfilePage extends StatelessWidget {
  final String? uid;
  const StudentProfilePage({super.key, this.uid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
